# 06-05 — Спрайты как клиентская сущность, `SPRITE_PLAYER`

**Этап:** 6 — Мультиплеерная логика локально
**Зависит от:** 06-02
**Блокирует:** 07-05
**Оценка:** ~100 строк изменений

## Контекст

`SpriteList` сейчас работает и как рендер-список, и как часть симуляции:

- `enemy.c:147-150` каждый тик копирует позицию врага в спрайт
- `enemy_damage()` (`enemy.c:164-169`) меняет тип спрайта на труп
- `enemy_spawn()` (`enemy.c:37`) создаёт спрайт и хранит `sprite_id`
- `item.c:47-49` деактивирует спрайт при подборе

На сервере спрайтов нет вовсе. Направление зависимости надо развернуть:
клиент строит спрайты из состояния мира, а не мир поддерживает спрайты.

## Что сделать

- [ ] Убрать `sprite_id` из `Enemy` (`enemy.h:34`) и `Item` (`item.h:18`);
      `enemy_spawn()` больше не трогает `SpriteList`
- [ ] Новая функция на стороне клиента:
  ```c
  void sprites_build_from_world(SpriteList *sl, const World *w, int local_player);
  ```
      Вызывается раз в кадр перед `sprite_render()`: `sprite_clear()` +
      добавление врагов (живой/труп по `state`), активных предметов,
      бочек и **чужих** игроков (локальный пропускается)
- [ ] Добавить `SPRITE_PLAYER` в `sprite.h:9-17`, дефолтный масштаб рядом
      с остальными (`sprite.c:15-24`), ветку в `sprite_texture()`
      (`sprite.c:43-54`)
- [ ] Процедурная текстура игрока в `assets.c` — по образцу `gen_enemy()`
      (`assets.c:118`); отличать от врагов цветом
- [ ] Бочки: сейчас статичны и живут только в спрайтах — перенести их в
      `World` как отдельный список либо оставить как декор, восстанавливаемый
      из карты при загрузке
- [ ] Убрать из `tests/test_enemy.c` заглушки `sprite_init()`/`sprite_add()`
      и проверки синхронизации спрайта с врагом, а из `tests/test_item.c` —
      расстановку предметов через `SpriteList`: после этой задачи ни `enemy.c`,
      ни `item.c` о спрайтах не знают, позиции берутся прямо из сущностей

## Затрагиваемые файлы

`sprite.h/.c`, `enemy.h/.c`, `item.h/.c`, `assets.h/.c`, `world.c`, `main.c`,
`tests/test_enemy.c`, `tests/test_item.c`

## Критерий готовности

- [ ] `grep -n "sprite" src/enemy.c src/item.c` — пусто
- [ ] Пересборка спрайтов каждый кадр не роняет FPS (список небольшой:
      `MAX_SPRITES 192` после 06-02, сортировка `qsort` в `sprite.c:82` —
      не узкое место даже при 10 игроках)
- [ ] Девять чужих игроков одновременно в кадре не переполняют `SpriteList`
- [ ] Второй игрок виден в мире как спрайт, корректно перекрывается стенами
      (Z-буфер) и другими спрайтами
- [ ] Трупы, предметы и бочки выглядят как раньше

## Замечания

Сортировка в `sprite_render()` (`sprite.c:70-92`) и Z-буфер продолжают
работать без изменений — меняется только источник данных.

Спрайт игрока без направления выглядит странно (все игроки «смотрят» на
камеру). В deathmatch это не косметика, а геймплей: невозможно понять,
заметил тебя противник или нет. Стоит заложить хотя бы 4 ракурса
(фронт/спина/два профиля) по разнице `angle` игрока и направления на камеру —
`gen_player()` рисует их процедурно, работа небольшая.

Цвет спрайта — по `id` игрока (палитра на 10 цветов), иначе в свалке
из 10 человек никого не различить.

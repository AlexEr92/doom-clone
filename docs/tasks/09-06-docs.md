# 09-06 — Обновить документацию

**Неделя:** 9 — Мультиплеерный геймплей и меню
**Зависит от:** 09-04
**Блокирует:** —
**Оценка:** ~100 строк

## Контекст

`README.md` описывает одиночную игру: возможности, зависимости (SDL2 +
SDL2_mixer), сборку, управление. После недель 5–9 он устарел по всем
разделам.

## Что сделать

- [ ] Раздел «Возможности» — добавить мультиплеер: до 4 игроков,
      client-server, co-op и deathmatch
- [ ] Зависимости — добавить ENet (таблица по платформам уже есть в
      [plan-multiplayer.md](../plan-multiplayer.md#зависимости)),
      упомянуть `-DVENDOR_ENET=ON` и `-DENABLE_MULTIPLAYER=OFF`
- [ ] Раздел «Сетевая игра»:
  - запуск хоста и клиента (из меню и флагами)
  - порт 27015/UDP, замечание про firewall и проброс портов
  - выделенный сервер `--dedicated`
  - что делать при `CONNECTION LOST`
- [ ] Управление — Tab (скорборд) и F3 (оверлей сети)
- [ ] Формат карты — маркеры `1`–`4`
- [ ] Раздел «Архитектура» — коротко: authoritative server, единый
      `world_step()`, снапшоты 20 Гц, предсказание и интерполяция.
      Полезно и как памятка при возврате к коду через полгода
- [ ] Отметить выполненные недели в `plan-multiplayer.md` (раздел «Статус»)
      и проставить чекбоксы в файлах задач
- [ ] Пройти [requirements.md](../requirements.md) раздел 10 и отметить
      выполненные критерии; расхождения между заявленными требованиями и
      фактическими замерами внести в документ, а не замолчать
- [ ] Обновить дерево проекта в `plan-multiplayer.md` под то, что
      получилось на самом деле (имена файлов почти наверняка разойдутся
      с планом)

## Затрагиваемые файлы

`README.md`, `CLAUDE.md`, `docs/plan-multiplayer.md`, `docs/tasks/*.md`

## Критерий готовности

- [ ] Человек с чистой машиной по README собирает проект и поднимает
      игру на двоих, ничего не спрашивая
- [ ] Все команды из README проверены копипастом, а не по памяти
- [ ] Статусы в плане и задачах соответствуют коду

## Замечания

Проверка README копипастом — не формальность: команды сборки в нём
устаревают первыми. Идеально — прогнать на чистом контейнере
(`docker run --rm -it debian:stable`), это заодно ловит незадекларированные
зависимости.

# 05-04 — Переписать хитскан на мировой рейкаст

**Этап:** 5 — Развязка симуляции и рендера
**Зависит от:** 05-03
**Блокирует:** всё остальное (07-04, 08-02, 08-05)
**Оценка:** ~120 строк изменений
**Приоритет:** критический

## Контекст

Это главный блокер мультиплеера. Сейчас попадание считается **в экранном
пространстве**:

- `weapon.c:109` — `float wallDist = zBuffer[screenX];` читает глобальный
  массив (`raycast.h:11`), заполняемый во время **рендера**
- `weapon.c:43-58` — `enemy_screen_band()` считает экранный AABB врага
  через `SCREEN_W` / `SCREEN_H`
- `weapon.c:102` — попадание ищется по номеру экранного столбца

Следствия: стрельбу нельзя посчитать без отрисовки кадра (на сервере нет
фреймбуфера), при нескольких игроках `zBuffer` принадлежит чужой камере, а
результат выстрела зависит от разрешения экрана.

## Что сделать

- [ ] Добавить в `raycast_world.h` трассировку по сущностям:
  ```c
  #define ENEMY_HIT_RADIUS  0.30f
  #define PLAYER_HIT_RADIUS 0.20f   /* = PLAYER_RADIUS */

  RayHit world_raycast(const Map *m, const DoorList *dl,
                       const EnemyList *el,
                       const PlayerState *players, int player_count,
                       float ox, float oy, float dx, float dy,
                       float maxdist, int ignore_player);
  ```
  - `raycast_walls()` даёт дистанцию до стены
  - пересечение луча с окружностью каждой живой сущности (квадратное
    уравнение), отбрасывая попадания дальше стены и позади начала луча
  - возвращается ближайшее
- [ ] Переписать `weapon_try_fire()` (`weapon.c:60-131`):
  - для каждой дробины повернуть направление на случайный угол разброса
    (`w->spread`) — как сейчас в `weapon.c:81-90`
  - вызвать `world_raycast()`, при `HIT_ENEMY` — `enemy_damage()`,
    при `HIT_PLAYER` — `player_damage()` с id стрелявшего
  - `ignore_player` = стреляющий, чтобы не попасть в себя
- [ ] `player_damage(PlayerState *victim, float dmg, int attacker_id,
      EventQueue *ev)` — учёт брони (тот же расчёт, что в `enemy.c:129-138`),
      запись `last_attacker` для атрибуции фрагов (09-03).
      PvP — основной геймплей в deathmatch, поэтому путь `HIT_PLAYER`
      делается сразу рабочим, а не заглушкой
- [ ] Удалить `enemy_screen_band()` и все обращения к `zBuffer` из `weapon.c`
- [ ] Убрать `#include "raycast.h"` из `weapon.c`

## Затрагиваемые файлы

`raycast_world.c/.h`, `weapon.c`

## Критерий готовности

- [ ] `grep -n "zBuffer\|SCREEN_" src/weapon.c` — пусто
- [ ] Пистолет попадает по врагу под прицелом, не пробивает стены и
      закрытые двери
- [ ] Дробовик на дистанции даёт частичные попадания (часть дробин мимо)
- [ ] Выстрел в другого игрока наносит урон с учётом брони; в себя попасть
      нельзя
- [ ] Результат выстрела не меняется при смене `SCREEN_W`/`SCREEN_H`
      в `utils.h:6-7` (временно поменять и проверить)

## Замечания

Радиус попадания по врагу подобрать по ощущениям: экранный AABB был шире
реального силуэта, так что честный радиус 0.3 может ощущаться как «стало
труднее попадать». Это нормально, но требует прогона на балансе.

`PLAYER_HIT_RADIUS` для deathmatch важнее, чем радиус врага: он напрямую
задаёт, насколько легко попадать по игрокам. Начать с `PLAYER_RADIUS` (0.2)
и подобрать при балансировке (09-05) — 0.2 может оказаться слишком строгим
для хитскана на дистанции.

`rand()` в `weapon.c:84` не сеется (`srand` нигде нет) — оставить как есть,
на этапе 7 разброс будет крутить только сервер (см. 07-04).
